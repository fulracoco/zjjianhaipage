{
  "name": "zhenhai-marine-parts-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.45.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.0",
    "wrangler": "^4.111.0"
  }
}
